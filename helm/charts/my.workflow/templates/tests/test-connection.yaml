apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my.workflow.fullname" . }}-test-connection"
  labels:
{{ include "my.workflow.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "my.workflow.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
